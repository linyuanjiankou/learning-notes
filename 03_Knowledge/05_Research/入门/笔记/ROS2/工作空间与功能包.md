# 基本概念
## 工作空间
> 存放项目开发相关文件的文件夹，开发过程的大本营
### 内置子空间
- src：代码空间
- install：安装空间
- build：变异空间
- log：日志空间
### 创建&编译工作空间
- 创建工作空间：
```shell
mkdir -p ~/my_dev/src && cd ~/my_dev/src
```
- 自动安装依赖
```shell
sudo rosdepc init && rosdepc update
cd ~/my_dev
rosdepc install -i --from-path src --rosdistro humble -y
```
- 编译工作空间
```shell
cd ~/my_dev
colcon build
```
- 设置环境变量
```shell
source install/local_setup.sh
```
> 关于设置环境变量的问题，理论上说，每次打开该项目都要执行一遍设置环境变量的指令，但是为了方便管理，有多种简易的管理方式写入.bashrc文件；
> 鉴于我目前项目较少，我使用 `alias myros='cd /media/zhou/B8822DF4822DB7B0/Profile/02_My_DevSpace/01_Lab/ROS2/my_dev && source install/local_setup.sh` ，
> 这种类似于取别名的方式进行，
> 每次只需要在终端输入 `mydev` 就可以设置该项目的环境变量，如果之后项目较多的话可以写入函数来管理。

